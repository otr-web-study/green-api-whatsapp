const HeaderContainer = ({ children }) => {
  return <header className="flex items-center px-4 bg-bg_gray h-[59px]">{children}</header>;
};

HeaderContainer.displayName = 'HeaderContainer';

export default HeaderContainer;
